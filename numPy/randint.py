import numpy as np

#random integer. 

num = np.random.randint(1, 100, 5) # random 5 integer from 1 to 100, 
print(num)

print(np.random.randint(1, 100))
print(np.random.randint(5))

print(np.random.rand(5))#see its rand, value from 0 to 1, probability 5 elements in teh list