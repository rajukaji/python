import numpy as np

lst = [1, 2, 3, 4, 5]

print(np.array(lst))#array of numpy

print(np.zeros(5))#the arguement is for number of zeros
#creates float array

print(np.ones(5))#create floating 1.'s up to 5 times array
