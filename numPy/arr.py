import numpy as np

# nd array similar to list

a = np.array([0, 1, 2, 3, 4, 5])

print(type(a))

print(a.size)
# number of elements in the array

print(a.ndim)
# n dimension

print(a.shape)
# size of the array in each dimension

print(np.org)